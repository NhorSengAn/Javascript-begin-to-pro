const arr = ["khmer", "check", "Make money", "search", "prey veng"];
let sum = 0; // must be `let`, since we're reassigning it

for (let i = 0; i < arr.length; i++) {
  sum += 1;
}

console.log(sum); // 5
